# Copy this file to .env and fill in your real Neon connection string.
# .env is git-ignored; .env.example is the committed template (no secrets).
#
# Neon connection strings require SSL -- keep the ?sslmode=require suffix.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
